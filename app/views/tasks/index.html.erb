<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task_path(task) do %>
      <%= task.title %>
    <% end %></li>
  <% end %>
</ul>

<%= link_to "New Task", new_task_path(@task) %>
